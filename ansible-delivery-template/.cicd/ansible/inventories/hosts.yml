all:
  hosts:
    localhost:
      ansible_connection: local
  children:
    # deploy.yml adds the configured target_servers to these runtime groups.
    delivery_kr_stg:
      hosts: {}
    delivery_kr_prd:
      hosts: {}
    delivery_eu_stg:
      hosts: {}
    delivery_eu_prd:
      hosts: {}
    delivery_na_stg:
      hosts: {}
    delivery_na_prd:
      hosts: {}
